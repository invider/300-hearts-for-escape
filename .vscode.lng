#!/bin/bash
SCRIPTPATH="$(cd "$(dirname "$0")" ; pwd -P )"
export WORKSPACE_DIR=$SCRIPTPATH
cd $SCRIPTPATH
LOGFILE=$WORKSPACE_DIR/lnglog.log
node ./intellisence/$1.js $@ > $LOGFILE 2>&1
cat $LOGFILE
echo $@ >> $LOGFILE
